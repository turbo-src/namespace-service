version: "3.0"

services:
  library:
    depends_on:
      - db
    ports:
      - 4003:4003
    expose:
      - "4003"
    build: ./
    environment:
      DOCKER_DATABASE_URL: postgresql://user:password@db:5433/namespace-db
    tty: true


  db:
    image: postgres:13.7-bullseye
    restart: always
    ports:
      - 5433:5432
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: namespace-db
    volumes:
      - db-data:/db-data/

volumes:
  db-data:
